function randomRange(myMin, myMax) {
  // Altere apenas o código abaixo desta linha
  const minMax = Math.floor(Math.random() * (myMax - myMin + 1)) + myMin
  return minMax;
  // Altere apenas o código acima desta linha
}